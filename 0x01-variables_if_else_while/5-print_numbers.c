#include <stdio.h>

/**
 * main - Entry point
 * author: HIRWA Jean Luc
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int a;

	for (a = 0 ; a < 10 ; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
